package client

import (
	"testing"
)

// The client expects a set of keys to already 
func setupTestEnvironment() {
}

// TestBasicClient does minimal testing of the client object.
func TestBasicClient(t *testing.T) {
}
